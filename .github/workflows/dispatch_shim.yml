name: manual-dispatch-shim

on:
  workflow_dispatch: {}

jobs:
  dispatch-shim:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: No-op
        run: echo "manual dispatch shim to trigger required checks"
