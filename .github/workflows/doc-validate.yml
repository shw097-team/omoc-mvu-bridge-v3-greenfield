name: doc-validate

on:
  pull_request:
  merge_group:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  doc_validate:
    name: doc-validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate docs
        run: |
          echo "Checking documentation..."
          # 若有 linter 加入這裡
