tool_path: opencode (TUI)
git_head: 90a5ca3bf675a83d90d60eed5f06879dfefe1a3c
OMOC_TS: 2026-02-23T17:59:49Z

RUNLOG BEGIN
1.1 git rev-parse HEAD (rc: 0)
1.2 ls -la .opencode (rc: 0)
1.3 find .opencode -maxdepth 3 -type f -print | sort (rc: 0)
1.4 Detect current agent dir (rc: 0)
1.5 Detect current MCP config (rc: 0)
2.1 Create .opencode/agents/ (rc: 0)
2.2 Create 4 agent files (rc: 0)
2.3 Update .opencode/opencode.jsonc with mcp stub (rc: 0)
3.1 Re-list agents (rc: 0)
3.2 Parse checks (rc: 0)
3.3 Show top-level keys (rc: 0)
RUNLOG END

FILES CHANGED
MM .opencode/opencode.jsonc
?? .opencode/agents/

DIFF
diff --git a/.opencode/opencode.jsonc b/.opencode/opencode.jsonc
index c08be00..81470a3 100644
--- a/.opencode/opencode.jsonc
+++ b/.opencode/opencode.jsonc
@@ -1,55 +1,65 @@
 {
   "$schema": "https://opencode.ai/config.json",
-  "theme": "opencode",
-  "autoupdate": false,
 
-  // 重要：鎖死 Kimi，避免 OmO agent model override 被 UI/config.model 覆蓋的已知問題
-  "model": "opencode/kimi-k2.5",
+  // 預設交互模型（不影響 Oh-My 內部 agent 指派）
+  "model": "github-copilot/gpt-5-mini",
+  "small_model": "github-copilot/gpt-5-mini",
 
-  // 釘版本：npm 顯示 oh-my-opencode 最新 3.8.0（2026-02-23 時點）
-  "plugin": ["oh-my-opencode@3.5.4"],
+  // Provider：憑證依官方流程用 /connect 或 auth login 注入（不要寫進 repo）
+  // Providers 文件：先 /connect，再於 config 宣告 provider options。
+  "provider": {
+    "github-copilot": {},
+    "openrouter": {
+      "options": {
+        "timeout": 600000,
+        "setCacheKey": true
+      }
+    }
+  },
 
-  // 規則注入（AGENTS.md）
-  "instructions": ["AGENTS.md"],
+  // MCP 伺服器配置：僅限本地或授權伺服器，不包含憑證
+  "mcp": {
+    "example-server": {
+      "command": "REDACTED_CMD",
+      "args": [],
+      "env": {}
+    }
+  },
 
-  // ============ MODE A：deny-by-default，且「完全不詢問」 ============
+  // 不詢問任何允許：全 permission 無 ask
+  // Permission keys 與 defaults：external_directory/doom_loop 預設會 ask，必須顯式 deny。
   "permission": {
     "*": "deny",
 
-    // Read-only 能力（不詢問）
+    // 必要工具：允許（不詢問）
     "read": "allow",
-    "list": "allow",
+    "edit": "allow",
     "glob": "allow",
     "grep": "allow",
-
-    // Todo 可用（依你的 OMOC 工具鏈慣例，保留；也不詢問）
+    "list": "allow",
+    "bash": "allow",
+    "task": "allow",
+    "skill": "allow",
     "todoread": "allow",
     "todowrite": "allow",
 
-    // Web 僅 SUPPORT-only：禁用避免外網成為執行相依（OMOC/藍圖與 SubP1 要求）
+    // 外部資料/網路：一律 deny（SSOT 防污染；Web 只能 SUPPORT-only 且走 seeds 流程）
     "webfetch": "deny",
     "websearch": "deny",
+    "codesearch": "deny",
 
-    // 禁外部目錄（避免越權掃 host）
+    // 會預設 ask 的 guard：直接 deny，避免任何 prompt
     "external_directory": "deny",
+    "doom_loop": "deny",
 
-    // Bash 白名單：不詢問；沒列到就直接 deny
-    "bash": {
-      "*": "deny",
-      "git *": "allow",
-      "rg *": "allow",
-      "jq *": "allow",
-      "opencode *": "allow",
-      "bash scripts/*": "allow"
-    },
-
-    // Edit 白名單：不詢問；只允許“工廠線/驗收線”允許改動面
-    "edit": {
-      "*": "deny",
-      ".github/workflows/**": "allow",
-      ".github/actions/**": "allow",
-      "scripts/**": "allow",
-      ".opencode/**": "allow"
-    }
-  }
+    // LSP：避免環境差異造成漂移（必要時再 TT 放行）
+    "lsp": "deny"
+  },
+
+  // 插槽化：Oh-My 僅 Quarantine/Support-only；並且必須釘死版本
+  // 版本避雷：不要用 3.1.9（已知 blank/freeze regression）。
+  "plugin": ["oh-my-opencode@3.5.4"],
+
+  // 可選：禁止自動更新造成 drift（若 OpenCode 支援該鍵，保留；否則 preflight 會 FAIL_CLOSED）
+  "share": "manual"
 }

RESULT MATRIX
agents_dir: present
agent_files: Coordinator.md, Fixer.md, Verifier.md, Evidence-Collector.md
mcp: present
parse: JSONC_OK

SHA256SUMS
3a11f295cf26d39bf823e0b3d09b993f5c0e0dedd877484c4af8a86c97576442  .opencode/opencode.jsonc
4273db3d08724db555908a9666a3ff39ee1910a9ae4bb075a73903d88ebf3512  .opencode/agents/Coordinator.md
b48ccfb54658c5f78d8951d02459eebe325b3ce84ed97311402a266f38a86977  .opencode/agents/Evidence-Collector.md
b5ff321e2f67891e5135bf7a69e5f253a42ab2224320dd36bbf9a2a53b41da51  .opencode/agents/Fixer.md
c4fa250f81391e1b9d7994137072942c63f1120eb9574d8a7185c786e2ce6f9f  .opencode/agents/Verifier.md

VERDICT: PASS
