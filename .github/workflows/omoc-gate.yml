name: OMOC Gate

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  omoc_gate:
    name: omoc-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Gate smoke
        run: |
          set -e
          echo "omoc-gate: smoke OK"
