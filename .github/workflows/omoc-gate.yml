name: omoc-gate
on: [pull_request, merge_group]
jobs:
  acceptance:
    name: OMOC Acceptance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/accept/cp3_accept.sh
