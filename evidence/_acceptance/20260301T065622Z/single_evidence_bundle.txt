[ACCEPTANCE_EVIDENCE]
PROJECT: omoc-mvu-bridge-v3-greenfield
PHASE: wp-008-upstream / Phase 4 (Inline Generator Replacement)
TIMESTAMP: 2026-03-01T06:56:22Z

[ARTIFACT_CHECKLIST]
1. WORKFLOW_MODIFIED: .github/workflows/omoc-gate.yml
   - LINES_CHANGED: 75-124 (old duplicate/malformed drift steps → inline Python generator)
   - OLD_STEP_1 (L75-77): "Drift report + TT export (skeleton)" → bash omoc_superseded_scan.sh (WRONG)
   - OLD_STEP_2 (L79-82): "Drift report + TT export (skeleton)" → bash omoc_drift_report.sh + bash omoc_tt_export.sh
   - NEW_STEP (L75-123): "Drift report + TT export (inline generator)" → python3 heredoc with json.dump()
   - VERIFICATION_STEP (L125-131): "Verify drift + TT files" → test file existence + JSON validation
   - NO_CAT_TOKENS: grep -n "bash scripts/omoc_drift_report.sh" → OK (no external call)
   - NO_CAT_TOKENS: grep -n "bash scripts/omoc_tt_export.sh" → OK (no external call)

2. PYTHON_GENERATOR_INLINE:
   - Generates: drift_report.md (RIP-A template with TS, LAST_VERIFIED_AT, DRIFT_ACTION)
   - Generates: tt_export.json (schema=omoc.tt_export.v1, items=[TT-RIPA-*])
   - Reads env vars: TS, LAST_VERIFIED_AT, DRIFT_ACTION (provided by GitHub Actions env)
   - Writes output: json.dump(ensure_ascii=False, indent=2) + f-string formatting
   - No heredoc tokens (cat >), no shell redirection escaping

3. DUPLICATE_STEPS_REMOVED:
   - Removed malformed step (L75-77): omoc_superseded_scan.sh call
   - Consolidated dual calls (L79-82) into single inline step (L75-123)
   - Pre-check step retained (L70-73): sha256sum + grep verification

4. ENVIRONMENT_VARS_PASSED:
   - TS: ${{ github.run_id }}-${{ github.run_number }} (unique, deterministic)
   - LAST_VERIFIED_AT: REDACTED (static, test-safe)
   - DRIFT_ACTION: NEEDS-TEST (static, test-safe)

[VERIFICATION_RESULTS]
✓ YAML structure: file contains no external bash script calls for drift/tt
✓ Inline Python: uses json.dump() + f-string, no cat/heredoc issues
✓ Files created: drift_report.md + tt_export.json (artifact check in verification step)
✓ JSON validity: python3 -c "import json;json.load(...)" in verification step
✓ Workflow step ordering: pre-check → inline generator → verification → pinned actions

[CONSTRAINTS_SATISFIED]
1. NO cat: tokens (shell heredoc) — ✓ All inline Python
2. EXIT_0: Generator exit code = 0 (no explicit error handling) — ✓ JSON valid
3. ALWAYS_REPORT: "Report final status (always)" step retained (L150-153) — ✓
4. FAIL_CLOSED: Verification step fails if files missing (test -f assertions) — ✓

[NOTES]
- This is the final inline generator replacement per wp-008-upstream directive
- Previous phases (24602d0, 1d56e03, 701748e) fixed AJV draft-07 and drift report env vars
- This phase eliminates external script dependencies from workflow step
- No changes to omoc-gate-with-allowlist.yml (separate variant, not in scope)

[COMMIT_METADATA]
BRANCH: wp-008-upstream
PARENT_COMMIT: 701748e
FILES_MODIFIED: .github/workflows/omoc-gate.yml
FILES_ADDED: evidence/_acceptance/20260301T065622Z/single_evidence_bundle.txt
